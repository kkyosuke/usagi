# 画面設計

> [ドキュメント目次](../README.md)

`usagi hop` で起動する TUI の画面設計をまとめたディレクトリです。全体像は
[../01-overview.md](../01-overview.md) を参照してください。

## 目次

| # | 画面 | ドキュメント | 役割 |
|---|---|---|---|
| 0 | 起動スプラッシュ（Splash） | [00-splash.md](00-splash.md) | 起動直後に一度だけ流れる装飾アニメーション。ウェルカム画面と同じうさぎ AA を表示し、遅れてタイトルがフェードインしてからウェルカム画面へ切り替わる |
| 1 | 起動画面（Welcome） | [01-welcome.md](01-welcome.md) | アプリ起動時のトップメニュー。Open / New / Config / Quit を選択する |
| 2 | プロジェクト選択画面（Open） | [02-open.md](02-open.md) | 登録済みワークスペースの一覧から開く対象を選ぶ |
| 3 | 新規プロジェクト画面（New） | [03-new.md](03-new.md) | リポジトリのクローン、または既存ディレクトリの登録でワークスペースを追加する |
| 4 | 設定画面（Config） | [04-config.md](04-config.md) | グローバル設定を編集する |
| 5 | ホーム画面（Home） | [home/README.md](home/README.md) | 選択したワークスペースのセッション一覧とコマンド入力を備えた、3 モードのメイン操作画面 |

> Open でワークスペースを選択するとホーム画面へ遷移します。ホーム画面は左ペインにセッション一覧、右ペインに
> 操作対象（選択のプレビュー／集中のアクション UI／没入の埋め込みターミナル）を表示し、**選択・集中・没入**の
> 3 モードを行き来します（起動直後の既定は**選択**）。ワークスペース全体のコマンド（`session` / `config` など）は、
> `:`（コロン）で開く**コマンドパレット（統括）**から実行します。詳細は [home/README.md](home/README.md) を参照してください。

## 画面遷移図

```text
            [起動スプラッシュ Splash]
                       │（数秒後・自動で遷移）
                       ▼
                 [起動画面 Welcome]
                       │
      ┌────────┬───────┼────────┬────────┐
   Open(o)   New(e)  Config(c)  Quit(q) / Esc / Ctrl+C
      │        │        │           │
      ▼        ▼        ▼           ▼
 [プロジェクト [新規      [設定        アプリ終了
   選択画面]   プロジェクト 画面] ──(Esc)──▶ 起動画面へ戻る
      │        画面]                （別れのボックスを
      │        │                    表示）
      │        ├─(作成成功)──────────▶ [ホーム画面 Home] へ
      │        └─(作成失敗)──────────▶ 起動画面へ戻る（通知を表示）
      │（ワークスペース選択）
      ▼
 [ホーム画面 Home] ──(Ctrl+C)──▶ アプリ終了（実行中なら確認モーダル）
   （3 モード: 選択/集中/没入。: でコマンドパレット）
      │
      └─(config コマンド)─▶ [設定画面 Config] ──(Esc / q)──▶ ホーム画面へ戻る
```

※ Open を選ぶとプロジェクト選択画面へ遷移し、ワークスペースを選択するとホーム画面へ進みます。
New を選ぶと新規プロジェクト画面へ、Config を選ぶと設定画面へ遷移します。新規プロジェクト画面で作成に成功すると
そのままホーム画面へ進み、失敗すると通知を出して起動画面へ戻ります。設定画面・新規プロジェクト画面で
`Esc` を押すと起動画面へ戻ります。ホーム画面は基底の[選択](home/02-layout.md#選択overview既定)では `Esc` で戻らず、
終了は `Ctrl+C` のみです（実行中のセッションがあれば確認モーダルを挟みます）。ホーム画面で `:` を押して開く
[コマンドパレット](home/01-modes.md#2-つの-modal)で `config` を実行すると設定画面へ遷移し、`Esc` でホーム画面へ戻ります。

## 設計メモ（共通）

- 配色は `src/presentation/theme.rs` の `Palette` で一括管理する。各画面は `accent` / `success` /
  `danger` / `warning` / `feature` / `info` の意味役割を使い、具体的な ANSI 色やタイトルフェード用
  ANSI-256 値、埋め込みターミナルのリンク RGB は同ファイルを正本とする。
- 中央寄せは「端末幅 − コンテンツ幅」を 2 で割った左パディングで実現（`centered_padding`）。
  幅は**表示桁（display width）**で測るため、CJK など全角グリフを含むタイトル・フッターも実際の
  描画幅で中央に寄ります。端末が狭い場合は `saturating_sub` によりパディング 0 にフォールバックし、
  さらにコンテンツが端末幅を超えるとき（狭い端末での長いタイトル・フッター）は**末尾を省略（`…`）して
  端末幅にクリップ**するので、行が折り返してフレームを崩すことはありません。
- フッターのキーヒントは端末幅を超えると、**先頭のモードタグと優先度の高いキーを残し、優先度の低い
  末尾のキーから順に落として `…` を付け**ます（全キーは `?` のチートシートで参照できます）。各ペインの行
  （左の一覧・右ペイン）も自分のペイン幅にクリップされるため、`│` の区切り線は常に同じ桁に留まります
  （レイアウトシフトなし）。
- **レイアウトシフト（CLS）を起こさない**を UI 全体の原則とする。出たり消えたりする要素（バッジ・
  マーカー・チップ・ヒント帯など）は、**無いときも同じ桁・行を空白で確保**して隣接要素の位置を固定し、
  可変長の値は**表示中の最大幅に右詰め**で揃える。要素が現れたり消えたりしても、周囲は動かない。具体例:
  セッション一覧のステータスラベル／メモ／リソース／差分／[PR](home/03-sidebar.md#pr-バッジ) の各列
  （[サイドバー](home/03-sidebar.md)）、レール折りたたみ時の行数一致とタイトルのアクティブ名欄、アクション
  UI の chevron 欄、コマンドパレットとヒント帯の固定高（[レイアウト](home/02-layout.md)）。
- 各画面の描画ロジックは `src/presentation/tui/<screen>/`（状態 `state.rs`・描画 `ui.rs`・
  イベントループ `event.rs` に分離。ホーム画面はさらにコマンド解析/補完を `command/` に分離）、
  端末制御は `src/presentation/tui/io/screen.rs` に実装されています。
- 各画面の再描画は `screen.rs` の `FramePainter` による**差分描画**です。`ui.rs` が返す
  「1 行 1 要素」のフレームを直前のフレームと比較し、**変化した行だけ**を ANSI で位置指定して
  書き換え、1 回の書き込みにまとめます。これにより、キー入力ごとの**ちらつき（フリッカー）が起きません**。初回描画と、モーダル・
  埋め込みターミナル・設定画面など**他画面が上書きした後**は `FramePainter::reset()` で画面全体を
  再描画します（描画差分の基準を破棄）。埋め込みターミナルも同じ差分ロジック（`diff_frame`）を
  共有し、加えて実カーソルをシェルの位置へ重ねます。カーソルの形状（DECSCUSR。ブロック／下線／
  バー）はアクティブなペインのプログラムが選んだものを毎回送り直すため、タブを切り替えると前のタブの
  形状が残らず、選択したペイン自身の形状に戻ります（ペインを離れると端末既定へ戻す）。
- 中央寄せ・うさぎ AA・タイトル/サブタイトル/フッターなどの共通描画は
  `src/presentation/tui/widgets/`（`mod.rs` の共通描画・`picker.rs`・`dir_picker.rs`）に集約しています。

---

関連: [コマンドリファレンス](../03-commands/README.md) ｜ [設定](../05-settings.md) ｜ [データ永続化](../data/README.md)
